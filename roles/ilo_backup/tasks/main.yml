---
- name: Perform backup operation on the iLO
  register: result
  hpe.ilo.ilo_manage:
    category: Manager
    command: iLOBackup
    baseuri: "{{ baseuri }}"
    username: "{{ username }}"
    password: "{{ password }}"

- name: Status
  ansible.builtin.debug:
    msg: "{{ result }}"

