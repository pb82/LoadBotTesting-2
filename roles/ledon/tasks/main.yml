---
# tasks file for ledon
- name: Turn LED on
  community.general.redfish_command:
    category: Chassis
    command: IndicatorLedOn
    baseuri: "{{ _baseuri_ }}"
    username: "{{ _username_ }}"
    password: "{{ _password_ }}"
